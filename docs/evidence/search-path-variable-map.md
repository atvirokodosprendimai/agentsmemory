# Search-path variable map — 2026-08-26

Every variable a SERVED recall reads, with the `file:line` where its value is actually
consumed, whether it LIMITS (raising it admits more) or ENABLES (turns a behaviour on),
what bounds it, and whether it is inert under the shipped configuration.

Produced by four parallel readers over the four path segments, followed by an adversarial
pass over every INERT claim. Shipped config at time of mapping:
`fusion=rrf lex-weight=n/a lex-norm=n/a closet-boost=0.00 rerank=on(pool=10,weight=0.50,norm=sigmoid) unit=memory evidence=lexical`.


## The correction this map produced

An earlier claim in the session — "`RERANK_POOL=10` is the real cap on the fetch" — is WRONG,
and the map is what caught it. Both knobs bind, at different stages:

- `candidateKFor(limit, …)` = `limit × 3` = **15** under the shipped `limit=5`. `RERANK_POOL`
  acts only as a FLOOR here (`if k < rerankPool { k = rerankPool }`), and 15 > 10, so it never
  raises the fetch at any `limit >= 4`. **The fetch width is set by `limit`.**
- `applyRerankWith` then does `pool := min(s.rerankPool, len(ranked))`, so only **10** of those
  15 candidates are cross-encoded. **The rerank width is set by `RERANK_POOL`.**

So raising `RERANK_POOL` alone cannot help — there are only 15 candidates to draw from — and
raising `limit` alone cannot help either, because the cross-encoder still sees 10. They have to
move together, which no single knob expresses.


## Correction, 2026-08-26 — this map was partly false within a day of being written

It originally cited `file:LINE`. A change that inserted about a hundred lines into
`internal/palace/service.go` left **all 34 of its service.go references pointing at unrelated
code** — at a `}`, at a blank line, at a `return nil`. The rank.go, memory_search.go, mine.go and
config.go citations were still exact, which is what makes the failure clear: nothing was wrong
with the document except that a line number is not a stable address.

Two citations were also wrong about the FILE, not just the line: `ChunkSize`, `ChunkOverlap` and
`ChunkMin` are defined in `internal/palace/chunk.go`, never in service.go.

Every citation is now a SINGLE IDENTIFIER — `internal/palace/service.go:candidateKFor` — and
`TestEvidenceCitationsResolve` fails when one stops resolving, when a line number creeps back, or
when a multi-word anchor appears. That is the same rule this product applies to its own memories:
`am_add_drawer` tells callers to paste the exact code rather than a line number, because "line
numbers move on every edit above them".

## The map

| variable | default | role | inert? | read at | what moves when it moves |
|---|---|---|---|---|---|
| `query 250-rune cap (bare literal, no constant)` | 250 runes | LIMITING | no | internal/palace/service.go:queryRune)-1005 | Raising it lets more of a long question reach the embedder, BM25 and the cross-encoder — the truncated string is what all three channels see (service. |
| `DefaultSearchLimit` | 5 | LIMITING | no | internal/mcpserver/drawers.go:DefaultSearchLimit| Sets the page size AND, through it, the retrieval pool: candidateK = limit × hybridCandidateMultiplier (service.go:1254). At 5 that is 15 candidates.  |
| `MaxSearchLimit` | 100 | LIMITING | no | internal/palace/service.go:MaxSearchLimit| The only clamp on a caller-supplied `limit`. The MCP layer does NOT clamp (drawers.go:636 passes the raw int), so this branch is reachable on any call |
| `ChunkSize` | 1600 runes | BOTH | no | WRITE: internal/palace/chunk.go:ChunkSize| On the write path it decides what one vector represents — raising it puts more text behind one embedding (coarser, fewer competing chunks per memory), |
| `ChunkOverlap` | 320 runes (20% of ChunkSize) | BOTH | no | WRITE: internal/palace/chunk.go:ChunkSize| On write, how much context adjacent chunks share — raising it makes a term near a boundary findable from either chunk, at the cost of duplicated stora |
| `ChunkMin` | 50 runes | LIMITING | no | internal/palace/chunk.go:ChunkSize, consumed at internal/palace/ | A trailing remnant shorter than this is folded into the previous chunk instead of being emitted as its own drawer. Raising it merges longer tails back |
| `MaxEmbedRunes` | 4000 runes | LIMITING | **YES — and now INERT: read by nothing** | internal/palace/chunk.go:MaxEmbedRunes | Documented as "refuses before the request is built" (teiembed.go:189) and as a guard on Service.Update. Re-checked 2026-09-01 (ADR-045 T1): `grep -rn MaxEmbedRunes --include=*.go` outside tests returns ONLY its own declaration and that comment — no code reads it, so the 4000-rune refusal does not exist. It was already dead before ADR-045: the guard went when ADR-038 T4 moved content updates onto supersede, and this row kept resolving only because the variable `finalContent` it cited still existed. T1 deleted that variable and TestEvidenceCitationsResolve caught the dangling pointer, which is the gate doing exactly its job |
| `EMBED_BACKEND (config.Config.EmbedBackend)` | "ollama" (config.go:362) | ENABLING | no | cmd/server/main.go:EmbedBackend), | Selects the whole embedding client. Under the shipped default the branch at main.go:1060 returns immediately with the Ollama client; setting it to "te |
| `EMBED_URL (config.Config.EmbedURL)` | "" (unset — absent from config.Default(), config.go:359-372) | ENABLING | **YES** | cmd/server/main.go:TrimSpace) | When EMBED_BACKEND=tei, this is the TEI base URL and startup REFUSES if it is empty (main.go:1064-1069) — deliberately no fallback to OllamaURL, becau |
| `OLLAMA_EMBED_MODEL (config.Config.OllamaEmbedModel)` | "bge-m3" (config.go:365) | ENABLING | no | cmd/server/main.go:OllamaURL, cfg.OllamaEmbedModel, cfg | Names the model that turns every query and every chunk into a vector — it decides dimensionality (bge-m3 → 1024, config.go:153-155), the semantic spac |
| `OLLAMA_URL (config.Config.OllamaURL)` | "http://localhost:11434" (config.go:364) | ENABLING | no | cmd/server/main.go:OllamaURL, cfg.OllamaEmbedModel, cfg → internal/embed/ollama/ollama.go:TrimRight, "/") + "/api/embed", | The endpoint every query embed hits. Unreachable here is the one failure recall cannot survive: SearchPage fails CLOSED on embed error (service.go:103 |
| `HTTP_TIMEOUT (config.Config.HTTPTimeout)` | 30s (config.go:366) | LIMITING | no | cmd/server/main.go:OllamaURL, cfg.OllamaEmbedModel, cfg / :1071 → internal/embed/ollama/olla | The wall-clock budget for the query-embed HTTP call, and thus the ceiling on how long segment 1 can take before recall fails closed. Raising it lets a |
| `embedding model input window (bge-m3, 8192 tokens)` | 8192 tokens — stated in prose only, never read | LIMITING | **YES** | NOWHERE. The repo's only embedding-server capability probe i | A real ceiling imposed by the model and invisible to the code: past it, TEI returns a prefix vector plus HTTP 200 (Truncate:true, teiembed.go:188/:309 |
| `TEI embedRequest.Truncate` | true (hardcoded, teiembed.go:188, set at :309) | ENABLING | **YES** | internal/embed/teiembed/teiembed.go:Truncate| Turns an over-long input into a shortened vector plus HTTP 200 instead of a 413 that would fail the whole batch — i.e. it converts a loud failure into |
| `SearchQuery.Context (MCP `context` argument)` | "" (drawers.go:638) | ENABLING | no | internal/palace/service.go:SearchQuery) rerankQuery(-962 | Sharpens cross-encoder ordering only: the field comment (service.go:940-945) is explicit that it deliberately does NOT touch the embedding, 'because w |
| `SearchQuery.RetrieveK (per-call) and Service.retrieveK (process, RETRIEVE_K / config.RetrieveK)` | 0 both — config.go:372 `RetrieveK: 0`; the MCP search tool sets no RetrieveK field at all (drawers.go:632-639) | LIMITING | **YES** | internal/palace/service.go:withRetrieveFloors| A per-call / per-process FLOOR on how many distinct memories are fetched before ranking. It widens only — a value below the candidateKFor formula is i |
| `hybridCandidateMultiplier` | 3 (internal/palace/rank.go:hybridCandidateMultiplier) | LIMITING | no | internal/palace/service.go:hybridCandidateMultiplier| Converts the page size into retrieval work: candidateK = limit × 3. Under shipped (limit=5) that is 15. Raising it fetches more candidates so a lexica |
| `DefaultMaxDistance` | 1.5 (internal/palace/service.go:DefaultMaxDistance) | LIMITING | no | internal/mcpserver/drawers.go:DefaultMaxDistance| Drops hits farther than this cosine distance; <=0 disables the filter (SearchQuery field comment, service.go:936). Listed here for completeness so the |
| `candidateK (return of candidateKFor)` | 15 under shipped (limit 5 * 3; the `k < rerankPool` raise is false) | LIMITING | no | computed internal/palace/service.go:Room| It is a target count of DISTINCT surviving memories, not a cap on chunks. The loop keeps doubling k until `distinct >= candidateK`, so raising it both |
| `RETRIEVE_K / --retrieve-k / config.RetrieveK / Service.retrieveK` | 0 | ENABLING | **YES** | internal/palace/service.go:withRetrieveFloors| A positive value replaces candidateK when larger, widening the retrieve floor WITHOUT changing the page size. It also multiplies through maxCandidateW |
| `SearchQuery.RetrieveK (per-call floor)` | 0 on every served recall | ENABLING | **YES** | internal/palace/service.go:withRetrieveFloors| Would let one call widen its own retrieve floor without changing the page. Nothing on the served path can set it. |
| `maxCandidateWidening` | 8 (unexported const; no flag, no env var) | LIMITING | no | internal/palace/memory_search.go:maxCandidateWidening| Caps the doubling loop at k >= candidateK*8, i.e. three doublings past the start (15 -> 30 -> 60 -> 120), so at most 4 vector searches and 4 GetMany r |
| `MaxDistance / DefaultMaxDistance` | 1.5 (const service.go:38; injected as the MCP tool default at drawers.go:637; 0 disables) | LIMITING | no | internal/palace/memory_search.go:MaxDistance| Two distinct jobs from one number. (a) Drops any survivor whose distance exceeds it, counted as scopeDrops.OverDistance and recorded once at service.g |
| `RERANK_POOL / --rerank-pool / Service.rerankPool` | 10 (config.go:367; docker-compose.full.yml:74 pins "10"; palace.DefaultRerankPool = 10 at service.go:53) | BOTH | no | internal/palace/service.go:Room| Two roles, and only one is live in this segment. (1) In candidateKFor it would RAISE the retrieve floor to the pool size so the cross-encoder is not h |
| `RERANK_WEIGHT / Service.rerankWeight (as the guard inside candidateKFor)` | 0.5 (config.go:368; docker-compose.full.yml sets RERANK_WEIGHT "0.5") | ENABLING | **YES** | internal/palace/service.go:candidateKFor| In THIS segment it does exactly one thing: gate the candidateKFor widening so that a reranker configured at weight 0 — where applyRerankWith returns b |
| `VECTOR_BACKEND / --vector-backend / config.VectorBackend` | "sqlite" (config.go:360); "chromem" when --local and unset (main.go:179-181); SHIPPED "qdrant" via docker-compose.full.yml:49 | ENABLING | no | cmd/server/main.go:VectorBackend| Selects what answers the widening loop's k-nearest query. sqlite = sqlitevec alone, brute-force full scan with the payload filter compared in Go after |
| `SEARCH_SCOPE / --search-scope / config.SearchScope` | "wing" (config.go:363); the shipped compose sets nothing, so "wing" | ENABLING | no | internal/config/config.go:Config) ScopeSearchToWing() bool | Decides what a recall naming no wing searches. "wing" (shipped): searchWingFor substitutes the registration's default_wing (server.go:357-359), so the |
| `The scope predicate (q.Wing / q.Room)` | Wing = the registration's default_wing under SEARCH_SCOPE=wing, "" when the caller passes "*" or the registration has none; Room = "" unless the caller passes one | LIMITING | no | internal/palace/service.go:func (s *Service) searchCandidates(-978 | Exact string equality on both keys, empty meaning unconstrained. It runs THREE times per recall against two different truths: once pushed into the ind |
| `default_wing (auth.DefaultWingFrom, per MCP registration)` | wing_agentmemories for this repo's registration (am_status: default_wing=wing_agentmemories, mode=local) | LIMITING | no | internal/mcpserver/server.go:DefaultWingFrom)), inside searchWingFor,  | It IS the shipped scope for any recall that names no wing: it becomes q.Wing, hence the Qdrant must-match clause and the drawerMatchesSearch compariso |
| `FUSION (env FUSION / --fusion / config.Config.Fusion)` | "rrf" (internal/config/config.go:Fusion,) | ENABLING | no | internal/palace/service.go:func (s *Service) fusionModeName( | Selects WHICH ranking function runs at all, and is the gate that makes BM25_WEIGHT and LEX_NORM inert. rrf -> rankRRF (rank.go:241), which fuses the v |
| `BM25_WEIGHT (env BM25_WEIGHT / --bm25-weight / config.Config.BM25Weight) -> s.bm25Auto, s.bm25IDF, s.bm25Base` | "auto" (internal/config/config.go:BM25Weight,); service fields default bm25Auto=true, bm25Base=hybridBM25Weight=0.4 (internal/palace/service.go:bm25Base) | LIMITING | **YES** | internal/palace/service.go:func (s *Service) WithBM25Weight( / :436 / :438 / :440 / :444 — | On the linear path it sets how much of the fused score the lexical channel may contribute: `auto` -> rankHybridAdaptiveNorm (service.go:440), weight = |
| `hybridBM25Weight (const 0.4, rank.go:31) — the value bm25Base falls back to` | 0.4 (internal/palace/rank.go:hybridBM25Weight), assigned to bm25Base at internal/palace/service.go:bm25Base| LIMITING | **YES** | internal/palace/rank.go:vecSimFromDistance) + b — `vectorWeight*vecSimFromDistan | The lexical half of the convex blend; vectorWeight is derived as 1-bm25Weight at rank.go:361 rather than being a second constant. Inherited verbatim f |
| `LexicalCoverage / adaptiveBM25Weight (the `auto` mechanism)` | on — bm25Auto=true at internal/palace/service.go:bm25Base| LIMITING | **YES** | internal/palace/rank.go:LexicalCoverage, docs) — `return base * LexicalCoverage | Scales the lexical weight per query by the share of query terms that appear in at least one candidate but not all of them (rank.go:400). Terms in NO c |
| `LexicalCoverageIDF (the `auto-idf` mechanism)` | off — bm25IDF=false (zero value; NewService at internal/palace/service.go:bm25Auto) | LIMITING | **YES** | internal/palace/rank.go:LexicalCoverageIDF`base*LexicalCoverageIDF | Same role as LexicalCoverage but each term contributes its BM25-style IDF normalised so a df=1 term is worth 1 and a term in every candidate ~0 (rank. |
| `LEX_NORM (env LEX_NORM / --lex-norm / config.Config.LexNorm) -> s.lexNorm` | "page-max" (internal/config/config.go:LexNorm,; DefaultLexNorm at internal/palace/rank.go:DefaultLexNorm) | LIMITING | **YES** | internal/palace/rank.go:lexical, ceiling) — `lexical := norm | Chooses the divisor that turns raw BM25 into the [0,1] lexical term. page-max (rank.go:512) divides by the page's best score, so the winner always rea |
| `rrfK (const 60.0, internal/palace/rank.go:rrfK)` | 60.0 — compile-time constant, no flag, no env var, no config.Config field | LIMITING | no | internal/palace/rank.go:fused)) and :264 | Two distinct effects, and both are live under the shipped config. (a) ORDER: fused = 1/(rrfK+vectorRank) + 1/(rrfK+lexicalRank). Raising rrfK compress |
| `CLOSET_BOOST (env CLOSET_BOOST / --closet-boost / config.Config.ClosetBoost) -> s.closetBoostScale` | 0.0 (internal/config/config.go:ClosetBoost,; mirrored as defaultClosetBoost at cmd/server/main.go:defaultClosetBoost). NOTE the service's own zero value is 1 (full prior, service.go:253), so main.go:862 applies the config UNCONDITIONALLY — a delta-only application would have shipped the prior this default exists to retire. | ENABLING | no | internal/palace/mine.go:scale`if scale == 0 { sp.End | Turns the entire closet curation channel on or off, not just its arithmetic. At 0 the recall skips a per-search vector search against the closet names |
| `closetRankBoosts (var {0.40, 0.25, 0.15, 0.08, 0.04}, internal/palace/rank.go:closetRankBoosts, 0.25, 0.15, 0.08, 0)` | {0.40, 0.25, 0.15, 0.08, 0.04}, from the frozen Python CLOSET_RANK_BOOSTS | BOTH | **YES** | internal/palace/mine.go:closetRankBoosts| LIMITING in magnitude — the boost the i-th-best closet lends every drawer from its source, diminishing by rank so only the best closet lifts meaningfu |
| `closetDistanceCap (const 0.6, internal/palace/rank.go:closetDistanceCap)` | 0.6 — compile-time constant, no flag, no env var | LIMITING | **YES** | internal/palace/rank.go:closetDistanceCap| The farthest a closet hit may be (cosine distance) and still lend its source any boost. Raising it admits more distant, less related closets as booste |
| `RECENCY_BAND -> s.recencyBand` | 0 — the Go zero value. NewService (internal/palace/service.go:bm25Auto) never assigns it. | ENABLING | **YES** | internal/palace/service.go:recencyBand`if s.recencyBand > 0`; co | Would turn on a date-preference tie-break: within a band of fused score, a newer content_date swaps two ADJACENT candidates (adjacent-only bubble at r |
| `RERANK_URL` | "" (empty — config.Default() at internal/config/config.go:OllamaEmbedModel, never assigns RerankURL, so it keeps the zero value; docker-compose.full.yml:55 sets it to http://reranker:8080/v1 in the shipped stack) | ENABLING | no | cmd/server/main.go:RerankURL| It is the on/off switch for this entire segment. Empty leaves s.rerank nil, so applyRerankWith bypasses at service.go:1305-1307 with ReasonNoReranker  |
| `RERANK_POOL` | 10 (internal/config/config.go:RerankPool,; mirrors palace.DefaultRerankPool at internal/palace/service.go:DefaultRerankPool, lowered from 50 on 2026-08-21) | LIMITING | no | internal/palace/service.go:rerankPool`pool := min | Caps how many fused candidates are cross-encoded at 10. Everything ranked 11th and below is never scored: it keeps its fused position and a zero Reran |
| `RERANK_WEIGHT` | 0.5 (internal/config/config.go:RerankWeight,; mirrors palace.DefaultRerankWeight at internal/palace/service.go:DefaultRerankWeight) | BOTH | no | internal/palace/service.go:rerankWeight`head[i].Blended = weight* | Above 0 it is LIMITING in the sense of a split: it sets how much of the final ordering the cross-encoder decides versus the fused lexical/vector score |
| `RERANK_NORM` | "sigmoid" (internal/config/config.go:RerankNorm,; mirrors palace.DefaultRerankNorm at internal/palace/service.go:func (s *Service) applyRerankWith() | ENABLING | no | internal/palace/service.go:normalizeBlendAxes| Selects which pair of normalisers brings the two blend axes onto a comparable scale. It governs BOTH axes, deliberately (service.go:1505-1512: "normal |
| `RERANK_TIMEOUT` | 10s (internal/config/config.go:RerankTimeout,; flipped from 90s per CHANGELOG.md:216) | LIMITING | no | cmd/server/main.go:RerankTimeout| Bounds the complete Rerank call, not each HTTP request — tei.go:57-61 records why: at pool 100 and a 10s per-request timeout, four batches of nine sec |
| `MEMORY_EVIDENCE_SELECTOR` | "lexical" (internal/config/config.go:MemoryEvidenceSelector,; mirrors palace.DefaultMemoryEvidenceSelector at internal/palace/service.go:memoryEvidenceSelector) | ENABLING | no | internal/palace/service.go:SearchHit, 0, limit) — `if s.MemoryEvidenceSelect | Chooses how a reassembled memory is reduced to the cross-encoder's bounded document. lexical (shipped): the branch is not taken, docs stay exactly wha |
| `maxMemoryEvidenceRegions` | 4 — computed as ChunkSize / DefaultSnippetChars = 1600 / 400 (internal/palace/memory_search.go:maxMemoryEvidenceRegions). Not operator-settable: a compile-time const with no flag and no env var. | LIMITING | no | internal/palace/memory_search.go:snippetRegions, query, ChunkSize, maxMe — `snippetRegions | Caps how many distinct matching passages of ONE memory share the 1600-rune cross-encoder budget, so each passage keeps roughly a normal snippet's wort |
| `snippet_chars (per-call tool argument; default palace.DefaultSnippetChars)` | 400 (internal/palace/rank.go:DefaultSnippetChars), applied as the tool-argument default at internal/mcpserver/drawers.go:snippetChars| LIMITING | no | internal/mcpserver/drawers.go:snippetChars| Bounds how much of each hit's text the RESPONSE carries. It is strictly post-ranking: it never touches retrieval, the rerank pool, the blend, or the p |
| `responseBudget` | 40_000 runes (internal/mcpserver/drawers.go:responseBudget). Not operator-settable: a compile-time const with no flag and no env var. Renamed from `wholeMemoryBudget` when am_list_drawers joined it as a second consumer. | LIMITING | no | internal/mcpserver/drawers.go:responseBudget`if snippetChars <= 0 && | Bounds the TOTAL content one tool response may carry, across search AND list_drawers. Records are filled in rank or listing order and the tail degrades to a bounded head — see headWithin, which bounds by what REMAINS so replacement heads cannot themselves overshoot the ceiling |
| `the page limit (DefaultSearchLimit / MaxSearchLimit)` | DefaultSearchLimit = 5, MaxSearchLimit = 100 (internal/palace/service.go:DefaultSearchLimit) | LIMITING | no | internal/palace/service.go:func (s *Service) rankRetrieved( — `if len | Two effects, both in the raising direction. (1) It is the final cut: memories that were retrieved, fused, cross-encoded and blended are discarded here |
